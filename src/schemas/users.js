const joi = require('joi');

const schemaBody = joi.object({
    // posision:joi.string(),
    // name:joi.string(),
    // ktp:joi.number(),
    // placeAndBrith:joi.string(),
    // gender:joi.string(),
    // religion:joi.string(),
    // status:joi.string(),
    // idCardAddress:joi.string(),
    // address:joi.string()
});


module.exports = {
    schemaBody
}